{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Opening a case file is the only ambient capability the viewer needs; everything else goes through explicit commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save"]
}
